# macOS
.DS_Store

# Screenshots de desenvolvimento (root and arbitrary subfolders)
*.png
*.jpg
*.jpeg
# Whitelist images under Assets/ (intentional product assets)
!Assets/**/*.png
!Assets/**/*.jpg
!Assets/**/*.jpeg
!Assets/og-image.jpg

# Playwright / browser automation
.playwright-mcp/

# gstack (dados locais de sessão)
.gstack/
.vercel

# Env vars (Notion token etc.)
.env
.env.*

# Node
node_modules/

# Claude Code (worktrees temporários de agentes)
.claude/worktrees/

# Pitch reference material (not committed — reference only)
intro outracoisa* outrai*.pdf
html intro outrai*/
